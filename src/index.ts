(() => {
    console.log("welcome to the console of my codebase!");
    
    // function test 1: sumOfarray(arr)

    // function test 2: findMax(arr)

    // function test 3: countVowels(str)

    // function test 4: reverseString(stri)
  })();